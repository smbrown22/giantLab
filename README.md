1. Command Three causes an error, because there's nothing wrong with creating the view, it just can no longer be updated when used 'with read only'.
2. If we want to restrict access to data, then preventative views need to be created to keep data in place.
3. Materialized views physically store the data, while regular views do not physically store the data. 
